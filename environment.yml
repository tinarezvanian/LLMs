name: subq
channels:
  - conda-forge
dependencies:
  - python=3.12
  - manim
  - ffmpeg
  - numpy
  - pip
  - pip:
      # Optional voiceover plugin — install after env exists: pip install manim-voiceover
      - openai
      - python-dotenv
      - tiktoken
