\chapter{Glossary}
\label{app:glossary}

A one-line definition for every term-of-art that appears in the book.
Alphabetical. Cross-referenced to the chapter where it is introduced.

\fillme{FILL-B-01}{Glossary entries A--D}{%
  \texttt{description} list with one-line definitions. Required entries (add
  more if the prose uses them): activation memory, ANN (approximate nearest
  neighbour), attention head, attention matrix, autoregressive, BPE,
  Bahdanau attention, Chinchilla, Chinchilla-optimal, content-dependent
  routing, convolutional view of SSMs, cross-entropy, dense attention,
  decoder-only, diffusion language model, dropout (mention only).}{%
  This book's chapters; the original papers cited.}{%
  ${\sim}15$ entries; one line each.}

\fillme{FILL-B-02}{Glossary entries E--L}{%
  Required: emergent abilities, embedding matrix, FFN, FlashAttention,
  GQA (grouped-query attention), Hyena, JEPA, Kaplan power law, kernel
  feature map, KV cache, latent dirichlet allocation (mention only),
  LayerNorm, linear attention, lost-in-the-middle, LSTM.}{%
  This book; the original papers.}{%
  ${\sim}15$ entries; one line each.}

\fillme{FILL-B-03}{Glossary entries M--R}{%
  Required: Mamba, Mistral, MoE (mixture of experts), MQA (multi-query
  attention), n-gram, nucleus sampling, paged attention, perplexity,
  position-fixed routing, Performer, pre-LN, RMSNorm, RNN, RoPE, RULER.}{%
  This book; the original papers.}{%
  ${\sim}15$ entries; one line each.}

\fillme{FILL-B-04}{Glossary entries S--Z}{%
  Required: SAMBA, SentencePiece, sliding-window attention, softmax,
  sparse attention, SSA (subquadratic sparse attention), state-space
  model, subword, top-\(k\) sampling, top-\(p\) sampling, transformer,
  WordPiece, YaRN.}{%
  This book; the original papers.}{%
  ${\sim}15$ entries; one line each.}
